# Copy to .env and adjust.
#
# The site has no database and no admin panel: content lives in src/data/*.json
# and project files live in public/files/. This is the only setting there is.

# Public base URL, used for sitemap.xml / canonical tags
NEXT_PUBLIC_SITE_URL="http://localhost:3100"
